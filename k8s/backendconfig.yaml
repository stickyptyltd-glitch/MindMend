apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: mindmend-backendconfig
  namespace: mindmend
spec:
  # Cloud Armor disabled - enable after creating policy with scripts/cloud-armor-setup.sh
  # securityPolicy:
  #   name: mindmend-armor-policy
  healthCheck:
    checkIntervalSec: 10
    port: 8000
    type: HTTP
    requestPath: /health

